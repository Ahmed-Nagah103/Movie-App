name: movie_app_clean_arc
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  carousel_slider:
  dartz:
  dio:
  equatable:
  flutter:
    sdk: flutter
  flutter_bloc:
  get:
  get_it:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
